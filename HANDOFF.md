# Tiny Conquerors — Session Handoff

## What this is
A mobile-friendly, browser-based RTS inspired by Age of Empires II: The Conquerors (original code and art — no Microsoft assets or names). Built for Daniel across many sessions; fully playable on phone (touch) and desktop (mouse + hotkeys). Since 2026-08-05 the game content is aligned to the REAL Conquerors manual (`C:\Users\deove\Downloads\Manual_AOE2_CONQ.pdf`, text pre-extracted to scratchpads via pdfplumber — Python 3.14 works on this machine now).

## Live artifact
- **URL:** https://claude.ai/code/artifact/2f3b1c5f-bcf7-4580-b292-afecb4f0fcbb
- **IMPORTANT for a new session:** to keep this URL when republishing, pass it as the `url` parameter to the Artifact tool (a conversation that didn't publish it otherwise mints a NEW url). Keep favicon "⚔️" and title "Tiny Conquerors".

## Source
- **Canonical source:** `C:\Users\deove\.claude\tiny-conquerors\tiny-conquerors.html` (single self-contained file, ~7,000 lines).
- The file has NO doctype/html/head/body — the artifact publisher wraps it. Starts with `<title>`, then `<style>`, HTML fragments, one big `<script>`.

## How to test locally (this machine)
- Use **`serve2.ps1`** — PowerShell HttpListener, `-Port` param (default **8742**), serves this folder AND accepts **POST /save?name=x.jpg** (body = base64 → written beside the server; the page can `fetch('/save?...',{method:'POST',body:b64})` to hand canvas screenshots to the session even when the Browser pane is hidden and `computer screenshot` times out). `serve.ps1` is the older fixed-8741 version, no /save.
- `.claude/launch.json` (at `C:\Users\deove\.claude\.claude\launch.json`) has a `tiny-conquerors` entry → `preview_start {name:'tiny-conquerors'}` starts serve2 on 8742.
- The raw file needs a wrapper for local testing (artifact wrapper adds doctype/viewport). Build `test.html` like:
  `$c=[IO.File]::ReadAllText("...\tiny-conquerors.html"); [IO.File]::WriteAllText("...\test.html","<!doctype html><html><head><meta charset=`"utf-8`"><meta name=`"viewport`" content=`"width=device-width, initial-scale=1, user-scalable=no`"></head><body>"+$c+"</body></html>")`
- Navigate to http://localhost:8742/test.html?v=N (bump N — cache-buster) in the Browser pane.
- **Headless testing pattern that works well:** `javascript_tool` → `document.getElementById('startBtn').click()` then call `step(0.05)` in loops to fast-forward the simulation (12k steps = 10 game-min), inspect `G` directly. `draw()` renders one frame. `G.vis.fill(2); fogDirty=true` reveals the map for screenshots. Buttons: `.diff button[data-d=0|1|2]` difficulty, `[data-m=0|1]` map, `[data-n=2..8]` players, `[data-t]` teams; set global `civSel` before clicking start. **Screenshots while the pane is hidden**: force-size the view (`vw=1280;vh=760;dpr=1;view.width=...;vig=...`), `centerOn` math is screen-space top-left: `p=isoPt(tx,ty); cam.x=p.x-vw/z/2`, then `draw()` + toDataURL + POST /save. Gallery-scene buildings need `queue:[],qt:0,gar:[]` or drawBld throws. A 10-min UNATTENDED sim usually ends in a loss → `G.over=true` → `saveGame()` refuses; test save/load on live games.

## Architecture (all in the one <script>)
- **Constants:** MAP=64/96 (let, by player count), TILE=26, AGES/AGE_COST (manual values; `ageCostOf(p,age)` applies civ discounts), **UNITS (~30 incl. 8 ships)** with `line:{}` per-age upgrade lines and `elite:{}` Imperial stats — **`statFor(type,p)` is the single stat resolver** (addUnit/atkOf/rangeOf/rofOf/unitName all use it), **CIVS (18, manual bonuses; first 12 order FROZEN for save compat)**, **UTECHS** (19 unique techs, castle-researched), **BS_TECHS** (blacksmith 5 lines × 3 tiers; armor = flat damage reduction via `bsArmorOf`), BLDS (tc/house/farm/camp/barracks/range/stable/siege/castle/tower/market/monastery/blacksmith/**dock**/walls/gates), DIFF, `IHT` sprite heights (**every new building type needs an entry or portraits crash**), `trainsFor(b,p)` = single source of trainable units.
- **State `G`:** units[], blds[], res{} (types wood/food/gold/**fish**), map[][], water{}, ford{}, **navBlock{}** (dock footprints — ships route around), elev, wallMap/gateMap, vis, P[NP] (res, age, civ, **uts[], bs{}, pt**), ais[], teams[], relics[], groups{}, fx/corpses/rubble/stuck/pings/proj, cam, sel[], inspect, placing/pend/wallA/pendLine.
- **Sim:** fixed 0.05s steps in `step(dt)`; `updateUnit` state machine (idle/move/attack/amove/toRes/gather/toFarm/farming/return/toBuild/building/toGar/toGarU/toHeal/heal/toRelic/toMon/**tradeOut/tradeBack**). A* `findPath(sx,sy,tx,ty,team,naval)` — **all unit repaths go through `uPath(u,tx,ty)`** (ships auto-naval via `passableW` = water/ford minus navBlock). Rams/transports carry passengers in `u.gar` (garrison finalized AFTER the unit loop — never splice mid-iteration).
- **AI (`aiThink`)**: villager jobs, houses (skipped for Huns), camps, military buildings by age, blacksmith + tech buying, dock near closest `G.waterList` tile (25s cooldown), unique-tech research at castle, fishing ships auto-fish, attack waves with persistent targets.
- **Rendering:** ISOMETRIC. Terrain flat-painted then skewed; buildings pre-rendered iso sprites (`ISO_ART`+`mkIsoSpr`); units procedural rigs (`drawManRig`/`drawHorseRig` + MAN_COSTUME/CAV_COSTUME, `drawWarWagon`/`drawShip` diagonal-snapped, `drawElephantRig`, `drawRam`), y-sorted with resource billboards (`getResSpr` incl. fish). Portraits render through drawUnitBody into cached dataURLs.
- **Sound:** WebAudio synth (`snd(kind)`), compressor bus, ambient birds; `initAudio()` on start tap.
- **Input:** tap select/command; right-click commands (mouse); box select; double-tap select-type; pinch/wheel zoom; minimap diamond tap; wall line placement; Ctrl+1-9 groups. `BUILD_KEYS` (blacksmith S, dock D), `TRAIN_KEYS_BY_BLD`, castle U = unique unit.

## Balance/behavior notes
- Stats follow the manual's Unit Attributes tables (pp. 42-44); lines upgrade by age; unique units go Elite at Imperial.
- High ground: +25% dmg attacking downhill, −20% uphill.
- Rams: building-only (2 vs units), arrows capped at 2 vs them; garrisoned infantry add +8% speed/+5 atk each.
- Counters: spearman +10 vs cav, skirmisher +5 vs ranged, mameluke +9 vs cav, jaguar +10 vs infantry, cataphract +6 vs infantry, samurai +5 vs unique units, fire ship +3 vs ships, huskarl/eagle arrowRes .5/.7, petard/demo self-destruct blasts.
- Blacksmith armor lines = flat -1 dmg per tier taken (min 1); Parthian Tactics -2 more for mounted archers.
- Wave pacing per DIFF; Squire deliberately gentle (firstWave 380s).
- Win/lose = destroy all enemy Town Halls (allies may survive).

## Hard-won gotchas (do not regress)
1. **Radius margins:** every "am I close enough" check needs slack for diagonal corners + crowd push (gather 1.8, farm 1.85, deposit size*.5+1.7, build size*.5+1.7, melee vs building 1.3). Tight radii caused villager deadlocks twice.
2. `canPlace(tx,ty,size,margin,allowFord)` — AI uses margin 1 so it never walls itself in; walls/gates use allowFord.
3. Sprite caches (`ISPR`/`RES_SPR`/`PORTRAITS`/`CORPSE_SPR`) are per-page-load and key on type/team/built/stage — art changes take effect on reload, no busting needed at runtime; but a NEW key dimension (like house `vr`) must be added to the cache key or variants collide.
4. Fog alphas tuned for phone visibility (unexplored 208, explored 84) after a "screen is all dark" complaint — don't darken again.
5. The artifact CSP blocks all external requests — everything must stay inline.

## Maps (added 2026-08-04, label "black-forest")
- Start screen has a **battlefield chooser** (`mapSel`, `MAPS[]`, buttons `.diff button[data-m]`, blurb `#mapBlurb`).
- `genMap()` dispatches → `genMapRiver()` (original) or `genMapForest()` (Black Forest: solid trees ~2500, carved base clearings r=8, center clearing with low hill + 6 gold, two corner pockets with gold, jittered trails width 1.6–2 connecting everything; verified base↔base pathable and AI reaches Castle Age + successfully attacks through the trails).
- **Minimap resource dots are cached** in `miniResC` canvas (rebuilt when `miniResDirty` — set in genMap and on resource depletion). Don't add per-frame `G.res` loops to drawMini; 2500 trees.
- `refreshTerrain` darkens ground under wood tiles (forest floor). Depleted-tree tiles keep the tint until next refreshTerrain — reads as cleared ground, intentional.

## Render app deployment — ✅ LIVE
- **https://tiny-conquerors.onrender.com** — Render static site `tiny-conquerors`, service ID `srv-d9pibf5bedkc73c5qumg`, connected to GitHub repo Danielparamount-rgb/tiny-conquerors, branch `main`, **publish directory `app`**, no build command. Auto-deploys on push to main.
- Current live version: **sw `tq-v23`** (deterministic-sim). Verified after every deploy by fetching sw.js + index.html from the shell (bypasses the cache-first SW).
- **Commit-message gotcha**: PowerShell here-strings choke on messages containing double quotes (`git commit -m @'...'@` split mid-message once). Use the Bash tool with `git commit -F - << 'EOF'` for multi-line messages.
- First verified live 2026-08-05 (tq-v1): SW active, manifest served as `application/manifest+json` (installable), icons OK, zero console errors.
- **GOTCHA — Render header rule for the manifest**: Render serves `.webmanifest` as `binary/octet-stream`, which can block Add-to-Home-Screen. Fixed with a dashboard Headers rule: path `/manifest.webmanifest`, name `Content-Type`, value `application/manifest+json`. Saving headers triggers a redeploy — during it files 404 inconsistently for ~30s; that's transient, re-check before diagnosing.
- **GOTCHA — testing the live site**: the SW is cache-first, so `fetch()` from the page returns CACHED responses (including stale Content-Type). To test the origin, unregister the SW + delete caches first, or the header change looks like it did nothing.
- **Git push now works non-interactively from this shell** — the GitHub credential is cached in Windows Credential Manager (verified `cmdkey /list` shows `LegacyGeneric:target=git:https://github.com`). Deploy updates without involving the user.
- Render dashboard SPA note: its tab can wedge (script-injection timeouts). Fix = open a NEW tab; also `form_input` wedged it once — prefer click-then-`computer type` on that site. Clicking a repo row needs the element `ref` (coordinate clicks just focus the search box).

## Render app deployment — history of getting there
- Goal: the game as its own PWA on Render like paramount-field-app.onrender.com. User signed into GitHub (**Danielparamount-rgb**) and Render in their real Chrome (drive via claude-in-chrome tools).
- **DONE 2026-08-04**: PWA bundle built in `app\` (index.html w/ PWA head + SW registration, manifest.webmanifest, sw.js cache-first VERSION 'tq-v1' — bump every deploy, icon-192/512.png shield art). Rebuild after any game change with `build-app.ps1`. Local SW test fails ONLY because serve.ps1 sends .js as text/html — fine on real hosting.
- **DONE**: repo created — https://github.com/Danielparamount-rgb/tiny-conquerors (PUBLIC, empty; public chosen deliberately so Render can deploy via "Public Git Repository" URL with zero OAuth grant screens; user can flip private later + connect Render's GitHub app).
- **REPO IS NOW A REAL LOCAL GIT REPO**: `C:\Users\deove\.claude\tiny-conquerors\` is `git init`'d on branch main, remote `origin` = the GitHub URL, user.name/email set repo-locally. `.gitignore` excludes test.html. Structure: source + docs at root, deployable bundle in `app/`. **Render publish directory is therefore `app`, NOT `.`**.
- 2026-08-05: user's own web drag uploaded the WRONG folder (parent, not `app\`) → remote commit 2cd0e74 has tiny-conquerors.html/HANDOFF.md/AOE2-REFERENCE.md/build-app.ps1/serve.ps1 at root. That's fine/kept. Local commit c9ec6f6 adds `app/` + README.md + .gitignore on top (linear, no force push). Also rebuilt app/index.html first — it was stale (predated the sound+UI polish).
- **CURRENT STEP — one-time auth**: `git push` from this shell fails ("Cannot prompt because user interactivity has been disabled"); no GitHub credential is stored in Windows Credential Manager (checked `cmdkey /list`), and GCM (installed, helper=manager) needs an interactive browser flow. Asked the user to run ONE command in their own PowerShell: `cd "C:\Users\deove\.claude\tiny-conquerors"; git push -u origin main` → GCM opens a browser → they authorize. **After that the credential is cached and all future pushes work from this shell non-interactively — deploy updates without bothering the user.**
- **THEN**: dashboard.render.com → New → Static Site → "Public Git Repository" → https://github.com/Danielparamount-rgb/tiny-conquerors → name tiny-conquerors, branch main, root dir blank, **publish directory `app`** → Create → wait for deploy → verify the live URL loads, SW registers, manifest installable. Update this section + memory with the live URL.
- **Automation dead-ends — do NOT retry** (2026-08-04): (1) claude-in-chrome `file_upload` is broken standalone (paths arrive undefined) and via browser_batch it filters ALL local paths as "not shared with this session" — including scratchpad; (2) fetch from github.com to localhost is blocked by GitHub's CSP connect-src; (3) popup+window.opener postMessage feeder is severed (COOP) — popupOpened:true but no message ever arrives, and subsequent popups get popup-blocked anyway; (4) hand-transcribing base64 chunks through tool calls corrupts (proven twice). Manual drag by the user is the reliable path for binary uploads; text-only files could go through GitHub's "create new file" editor as a fallback.
- Also beware on github.com: `computer type` keystrokes can trigger GitHub hotkey navigation if field focus is lost — use `form_input` for GitHub form fields.
- Deploy checklist for future updates: edit tiny-conquerors.html → run `build-app.ps1` (MANDATORY — app/index.html is a generated copy and goes stale silently) → bump VERSION in app/sw.js ('tq-v2', ...) → `git add -A; git commit; git push` → Render auto-deploys on push to main.
- IP boundary told to user (they asked for "exactly like AoE2", "just for friends"): imitate style with ORIGINAL art/sounds only; no ripped Microsoft assets, especially once hosted on Render.

## MULTIPLAYER — in progress (user goal: play with friends, the "huge thing")
Plan agreed 2026-08-05: lockstep — peers exchange COMMANDS, not state, and each runs the identical sim. Staged so every step is useful alone.
1. ✅ **Determinism** (sw tq-v23, commit 0df413b) — done, see below.
2. ⬜ **Command layer**: funnel every player action through one `issue(cmd)` chokepoint that appends to a per-tick command queue instead of mutating G directly. Today `handleTap` calls cmdMove/cmdGather/etc. inline — those calls ARE the commands, so wrap them. Then a local game is just "apply my own commands immediately".
3. ⬜ **Relay server**: tiny Node+ws service on Render (separate service from the static site), rooms by code, forwards `{tick,playerId,cmds}` blobs. No game logic.
4. ⬜ **Lockstep loop**: run the sim N ticks behind, only advancing when every peer's commands for that tick have arrived; compare `stateHash()` each second and halt with a clear message on mismatch.
5. ⬜ Lobby/teams/reconnect.
**Key facts for whoever continues:**
- `SR()`/`SRi(n)` = the ONLY randomness allowed to affect G. `seedSim(s)` sets it; `gameSeed` holds the seed and is saved. Cosmetic randomness stays on `Math.random()` deliberately (75 of the 84 sites) — peers may differ there.
- **`uid=1` reset in newGame is load-bearing** — commands reference units by id and `creepToward` reads `u.id%4`. Removing it desyncs peers.
- `stateHash()` ignores fx/corpses/rubble/camera/selection on purpose. If you make a cosmetic thing affect gameplay, add it to the hash.
- Determinism test method: set `pendingSeed`, call `newGame()`, step N times, compare `stateHash()`. Verified identical on all 3 maps and 8 players. **Re-run after ANY sim change.**
- Watch out: `Object.keys(G.res)` ordering, `Array.sort` stability, and `Date.now()` would all break lockstep. None are currently used in the sim.

## Recent fixes (last published state)
- Label "deterministic-sim" (2026-08-05, sw tq-v23, commit 0df413b): multiplayer step 1 — see the section above. Also fixed a real desync bug (`uid` never reset between matches).
- Label "8p-performance" (2026-08-05, sw tq-v22, commit 1641fc1): user reported 8-player games "a little glitchy" after the map-size bump. Profiled, three real costs:
  - **Separation was O(n²)** — the biggest sim cost (188 units ≈ 17k pair tests per step, growing with the square of army size). Now bucketed into `sepGrid` (Map keyed `x|0+','+y|0`), each cell tested against itself + `FWD=[[1,0],[-1,1],[0,1],[1,1]]` (forward-half stencil = every pair exactly once). **Verified by an exhaustive coverage test on a live 689-unit state: 112/112 pairs found, 0 missed, 0 duplicated.** Results differ from the old sweep by ≤4e-3 tiles purely from push ORDER — harmless for a settling force.
  - **Mist throttle**: the 512px `wispC` rebuild (9 composites) now runs every 3rd frame via `wispT`, plus immediately when `fogWasDirty` (set where `fogDirty=false`). Zoomed-out draw **6.5ms → 2.8ms**.
  - **Villagers stacked on shared resources** (a v17 creepToward regression): creep pulled every gatherer to the same point while separation's counter-push was blocked by the tree itself, so they compressed to ~0.003 tiles apart and jittered. Fixed with `crowdedAt()` (9-cell lookup against `sepGrid`, rejects a creep that lands within .34 of another unit) plus a per-unit `want+=(u.id%4)*.17` so crowds ring the trunk in bands. **10 villagers on one tree: closest pair 0.003 → 0.207, zero overlaps, all 10 still working.**
  - **Testing note**: full 8p sims exceed the 30s JS-tool limit — run ≤3 game-minutes per call, or use a targeted scenario (the 10-villagers-one-tree test found the bug that whole-game overlap counts obscured).
- Label "bigger-maps" (2026-08-05, sw tq-v21, commit c198e1d): user asked for much bigger, more detailed maps with clumpier trees.
  - **Sizes**: `setMapSize(mapSel===1?(NP<=4?96:112):(NP<=2?96:NP<=4?112:128))`. **128 is the hard ceiling** — terrain canvas is `MAP*TILE*TREZT` square, so 144→3744px and 160→4160px, and phones cap canvas dimensions at 4096px. Measured: 128@TREZT1 = 3328px / 42MB. Black Forest stays one size down (it fills every non-clearing tile; 12k trees at 128 made the whole-map view 44.5ms — at 112 it's 10.5ms).
  - **A* budget** now `Math.max(7000,MAP*MAP*.75)` — the old fixed 7000 could not path across a 128 board.
  - **`placeForest(cx,cy,n,minStart)`**: grows a dense blob by splicing a RANDOM frontier tile each step (random order = ragged lobes; sequential = circles) and pushing 8-neighbours at p=.74. River uses `11*MAP²/4096` forests of 26–78; Hallowed `8*MAP²/4096` of 16–46. Clumping metric (avg orthogonal neighbours per tree): **0.5 → 2.8**. Re-run that metric if you retune.
  - **Doodads** widened ~15%→~24% of tiles (bands in the refreshTerrain per-tile loop: tufts <11, flowers <15, pebbles <18, bush <21, rock <23, log ===23).
  - **Two bugs fixed**: (1) `Math.sign(MAP/2-bx)` returns **0** for a start exactly on the centre axis — the woodline `bx+dx,by+9*dy` then collapsed onto the Town Hall; hit 4 of 8 ring starts. Now `||1` (genMapHallowed already had it). (2) `genMap` now strips any resource from every TC footprint + 1-tile ring after generation, whichever generator ran — cheaper than auditing each path.
  - Verified 8p on all three maps: 0 blocked TCs, all AI economies 0-idle, whole-map frames 19.8/10.5/7.9ms, save/load + cross-map path OK.
- Label "sound-pass" (2026-08-05, sw tq-v20, commit 993ad4b): audio had gone untouched since before siege/gunpowder/monks/collapse existed — cannons were silent, conversion played the age fanfare, buildings fell without a sound.
  - New synth voices beside the existing ones: **`sBoom`** (sub-bass thump, sine+triangle), **`sCreak`** (sawtooth through a sweeping high-Q bandpass = rope under load), **`sChant`** (stacked fifths through a vowel formant).
  - New kinds: `gun`, `cannon`, `treb`, `sling`, `twang`, `crash`, `collapse`, `convert` (all with throttle gaps in the `gap` map — add one for any new kind or it inherits 120ms).
  - Wiring: the attack case plays the launch report keyed off unit flags (gun/siege→cannon, gun→gun, treb, blast→sling, pierce→twang, else arrow); `dealDamage` maps `ranged==='siege'`→`crash`; `removeBld`→`collapse`; `finishConvert`→`convert`.
  - **Positional mix**: `snd(kind,wx,wy)` sets module-level `sVolMul`/`sPan` which `sEnv` applies (and routes through a StereoPanner). **GOTCHA: `isoE` returns PRE-camera coords — draw() applies the camera via ctx transform. Convert with `(p.x-G.cam.x)*G.cam.z` before measuring screen distance.** Got this wrong first pass; the test caught it. Events >2.6 screen-radii away are dropped entirely. Calls with no coords (UI) stay full volume/centred.
  - **Test technique worth reusing**: render a sound through an `OfflineAudioContext` and measure peak/RMS. `snd()` early-returns on `AC.state==='suspended'` and offline contexts report exactly that — stub it with `Object.defineProperty(off,'state',{get:()=>'running'})` or everything measures silent. setTimeout'd stages don't render offline, so treb/convert/collapse under-report. New sounds should land at peak .05–.14 to sit with chop (.067) and sword (.10).
- Label "combat-weight" (2026-08-05, sw tq-v19, commit d81fdd6): three impact effects.
  - **Building collapse**: rubble records gained `type` + `tilt`; the rubble draw loop paints the *building sprite* buckling for the first .7s (rotate `tilt*.13*f²`, scale y by `1-.72f²`, anchored at the SOUTH corner exactly like drawBld's 1.13 scale — keep those in sync if you change one). Rubble then fades in over `(age-.55)/.35`. Dust burst widened to 14 outward-rolling puffs.
  - **Arrows in bodies**: arrow projectiles carry `tid` (unit targets only, no siege/gun kinds); on a non-miss landing a `G.stuck` entry with `{uid,ox,oy}` is pushed. The stuck draw loop re-syncs `x/y` from the live unit each frame and zeroes `t` when the victim dies — that's what prevents orphans (verified 0 after a 12-min 4p game).
  - **Melee recoil**: in the attack case, non-ranged hits ≥8 dmg shove the victim along the attacker→target vector by `min(.16,.012*dmg)`, gated on `walkTile` so nothing is pushed into terrain. Ships exempt.
  - Bounds after 12-min 4p Warlord: fx 9, stuck 1/50, rubble 0/40, corpses 17/140, 0 errors.
- Label "death-animation" (2026-08-05, sw tq-v18, commit 4141d53): units topple instead of popping to a corpse.
  - Corpse records now carry `hdg`, `face`, `fall` (±1) captured at death (in the dead-cleanup block of step()).
  - `drawCorpse` first 0.5s: draws the **living rig** via `drawUnitBody` on a fake unit (same trick portraitFor uses), rotating `fall*1.32*smoothstep`, sinking 2.2px, scaling 1.18 (matching drawUnit) and flattening 30%. **Stepped in 6 hard poses on purpose — period sheets ran ~12fps; do not smooth this out.**
  - Ships/rams skip it (wreck sprites) and corpses lacking `hdg` (older saves) fall back to the previous squash — that fallback is why nothing throws; keep it.
  - Corpses are never serialized, so save/load is unaffected. Cost: 25 simultaneous deaths = 1.2ms/frame.
- Label "closer-engagement" (2026-08-05, sw tq-v17, commit b7aabfa): user — units stood too far from what they gather/fight.
  - **`creepToward(u,tx,ty,want,dt)`** (defined just above targetEnt): after the state machine has ARRIVED, walk the last stretch directly at .55×speed with `walkTile` as the limiter (slides on one axis if blocked). **This is the safe way to close distance — the A* arrival radii (gather 1.8 / farm 1.85 / build size*.5+1.7) are UNCHANGED, so gotcha 1's deadlock protection still holds.** Called from gather (.95), farming (.62), building (size*.5+.55), and melee attack.
  - Melee engagement: `rng` for units .65→.62, buildings 1.3→1.25, and **only when `rangeOf(u)<=1`** does the attack case creep in (unit .44, building rad+.5). Ranged/siege never creep — verified an archer holds 5.26 with range 5.
  - **Villager tool stroke** rebuilt in the `weapon==='tool'` branch: piecewise phase (0–.18 settle, .18–.80 lift, .80–1 snap) so the blade bottoms out exactly as `gatherT` wraps and spawns the chips. **If you retune gather timing, keep the strike phase aligned to the wrap or the axe stops connecting.**
  - Idle units breathe: drawManRig bob falls back to `sin(t*.55+id*1.7)*.22` when stationary.
  - Deadlock canaries (re-run these after ANY radius change): 14 villagers → one tree = 0 stuck; 15-min Warlord = AI Castle Age, 13 villagers, **0 idle**, 28 buildings, 0 errors, frame 1.0ms.
- Label "quality-of-life" (2026-08-05, sw tq-v16, commit e7d97c9): the five suggestions from the tq-v15 audit, all shipped.
  - **`#idleBtn`** in the zoomBtns cluster; `updateIdleBtn()` called from `updateOverlays()` (1/sec throttle) toggles `.live` + count badge. Cheap: one filter over G.units per second.
  - **Floating tallies**: new fx kind `'text'` (fields `txt`,`col`; `drawFx` early-returns for it with stroke-then-fill so it reads over any terrain). Pushed in the `return` state when carry is banked, player 0 + visible only. `RES_COL` map added beside RES_META.
  - **Minimap**: `miniToTile(e)` extracted; pointerdown = jump + arm drag (`miniDrag`, pointer capture), pointermove = continuous scan, double-tap (<330ms) = push to `G.pings`. **Gotcha: a tap immediately followed by a drag reads as a double-tap — that's intended double-tap semantics, don't "fix" it.**
  - **Age-up fanfare**: `G.ageFx=1.4` + 22 gold chip sparks at the TC; draw() paints a radial warm bloom and decays `G.ageFx` by 1/60 per frame (transient, never serialized).
  - **`#panelBtn`** toggles `#panel.hidden` (display:none) → +201px map on a phone (~36% more); calls `chkView()` so the canvas recalibrates instantly and clicks stay true.
  - Verified: 8-min sim 276ms, frame 1.4ms, fx array stable at 15 (no leak), pings capped at 6, save/load clean.
- Label "phone-ui-pass" (2026-08-05, sw tq-v15, commit 4ef734b): full UI audit at 375x812. **Measured problems**: start screen 2506px tall / 618 words / 3.4 screens with `#startBtn` at offsetTop **2421px**; villager build menu showed **10 of 20** buildings. Fixes:
  - Start overlay restructured: title → goal → `#setupBlock` (civ/map/foe/players/teams/turbo) → Start/Campaign/Continue → rule → `<details id="howto">` **closed by default**, state in `localStorage['tq_howto']`. Page now **779px**, Start fully on-screen at phone AND desktop (desktop needed the compaction rules to be global, not inside the 560px media query — watch that if adding setup rows).
  - `<details id="civPick">` wraps `#civGrid`; summary shows `#civPickName`, and picking a civ closes it (civGrid button onclick).
  - **`BUILD_TABS` {eco,mil,def} + `buildTab` global**: three tab buttons rendered at the top of the villager branch of refreshPanel (styled inline to 26px so they share one grid row). Hotkeys bypass tabs entirely (startBuildKey unchanged) — verified cross-tab. Coverage test: every BLDS build entry appears in exactly one tab; **add new buildings to a tab or they become unreachable by touch**.
  - Phone tap targets: `.diff button` padding 11px, `.civgrid button` 3-across, `#btnRow` max-height 124→152px.
- Label "persistent-deselect" (2026-08-05, sw tq-v14, commit 3fe602d): user wanted deselect always reachable (the old one was the LAST button in the scrolling `#btnRow` grid). Added `#deselBtn` (✕) as a 5th button in `#zoomBtns` — floats top-right on phones, docks into the panel with the minimap on ≥760px via the existing placeMini(). `clearSel()` cancels placement first, else clears sel/inspect/amode + disarms box select; `updateDeselBtn()` (called at the top of refreshPanel) toggles `.live` styling, writes a count badge for multi-select, and updates the title. Removed the buried in-panel Deselect (Stop stays; the two inspect-branch Deselect buttons stay — those panels don't scroll).
- Label "hover-flicker-fix" (2026-08-05, sw tq-v13, commit f0b7e21): user — "very glitchy when I go over the lower portion." Cause was the click-offset fix's own recalibration in a feedback loop: mkBtn's hover tip writes into `#scrollHint`, a long tip wrapped to an extra line, the panel grew → `#viewwrap` shrank → chkView called resize() → canvas reallocated (blank for a frame) → tip cleared on mouseleave → panel shrank back → resize again. Sweeping the mouse across buttons ran that loop continuously. Fix: (1) `#scrollHint{min-height:29px}` reserves two lines so hover text can NEVER change panel height; (2) chkView() now calls draw() immediately after resize() so a reallocated canvas never presents empty. **Gotcha: any panel element whose content changes on hover/selection must have a reserved height, or it re-enters this loop.**
- Label "age-tier-art" (2026-08-05, sw tq-v12, commit eeca4d8): user reference-shot feedback — "larger, much more textured, looks different per age."
  - **Buildings draw 13% larger** (`BS=1.13` in drawBld, thin/farm exempt), anchored at the SOUTH corner (`southY=pt.y+2*size*IH`; top-left = `southY-(sp.oy+2*size*IH)*BS`) so bases/footprints stay put. Gameplay footprints unchanged.
  - **Grain denser**: spriteGrain divisor 26→19, buildings pass strength 1.35.
  - **3-tier age looks**: getBldSpr `ab` is now 0 (Dark/Feudal thatch) / 1 (Castle shingle) / 2 (Imperial). ISPR keys already carried ab; portraitFor's bucket updated to match. Imperial art: houses = STONE walls + masonry + HROOF[2] slate + chimney; tc = slate roof + gilded ridge cap/finials; barracks = slate + 3 team pennants on the ridge. **Pattern for future painters: take `ab` and branch; the cache handles the rest. Roof choice `ab===2?HROOF[2]:TERRA` is the house rule.**
  - Verified: Dark-vs-Imperial comparison screenshots (age-dark/age-imp.jpg beside serve2), 0 errors.
- Label "prerender-feel" (2026-08-05, sw tq-v11, commit 5705e9a): user (with a period-RTS screenshot as style reference) confirmed the palette is right but wanted less cartoon bounce + more pre-rendered surface texture. Two changes:
  - **`spriteGrain(c,seed,strength)`** (above mkIsoSpr): seeded dither speckle composited `source-atop` (only lands on painted pixels), applied ONCE at cache build in mkIsoSpr (all buildings) and getResSpr's billboard path (trees/gold/berries; fish branch returns early and is untouched). Density c.w*c.h/26, 1.4px specks, warm-dark/pale pairs. Runtime cost zero — warm frame 3.1ms.
  - **Motion damping** toward stiff sprite-era movement: drawManRig walk bob .55→.3, melee lunge 4.2→2.4, tree sway .02→.007 & slower (still gated by z>=.9 + <320 trees), ship bob .8→.45. Ram slam untouched (mechanical).
  - Gotcha: any NEW building painter automatically gets grain via mkIsoSpr; billboards added to getResSpr get it only if they flow to the shared return at the bottom (fish-style early returns skip it — decide per case).
- Label "ghost-silhouettes" (2026-08-05, sw tq-v10, commit e5606da): friendly units occluded by buildings/tree canopies redraw as flat team-color silhouettes (42% alpha) via `drawGhosts()` (called in draw() right before projectiles). `unitOccluded(u,px,py)` is a screen-space test: building sprites = foot corner `isoE(tx+size,ty+size)` painted-after check + IHT height box (skips sprites <30 tall — walls/farms); tree canopies = wood-res tiles with tile-sum offset 1..4, ±20px screen x, 1..52px below. Silhouette = drawUnitBody into a shared 110×124 offscreen (global `ctx` swap like portraitFor) → 'source-in' team fill → alpha .42 blit. Allied units only (no enemy intel through fog). ~Zero cost when nothing is occluded.
- **Engine project PARKED at step 2** (user: "that's worse, go back to the old game") — `..\tiny-engine\` stays as-is; only graft engine-spec ideas into the game when they fit seamlessly (this label is the first: the doppelganger/outline concept).
- Label "cd-round-2" (2026-08-05, sw tq-v9, commit a5e8c11): second CD mining pass (READMEX 1.0B balance notes + WHATSNEW Turbo rules + HALLOWED.RMS design parameters).
  - **1.0B balance** (see commit msg for full list): new civ hook fields `freeTech:{}` (mkP seeds eco — Aztec loom, Byz watch), `infCostAge` (Goth discount gates on age), `gunRof` (Spanish gunpowder+conquistador rof ×.85). Archers +1 vs spearman line & Parthian cav-archers +3 vs spears live in the attack case. Scout Feudal line atk 3→5. Castle bt 50. Shipwright also ×.85 ship train time.
  - **Turbo mode**: `turboSel` global + `TB()` helper (2.5 or 1) + start-screen `data-tb` row. Hooks: gather rate ×TB, farm interval ÷TB, carryCap ×TB, build rate ×2.5 (EXCEPT castle/tower/wonder), trainTimeOf ÷2.5 for non-econ units, cog speed ×2. Saved in save v1 (`turboSel`), loader defaults 0.
  - **Hallowed Ground** (mapSel 2, `genMapHallowed`): open steppe, ~8 ponds (fish in each; docks CAN sit on bigger ponds), hills, sparse forest clumps, per-player resources + center gold. placeRelics scatters **8 relics** (min 8 from starts, 7 apart). newGame gives every player a prebuilt monastery + monk when mapSel===2&&!mission. Relic-victory countdown pairs naturally with this map.
  - **AI relic circuit**: AI monks (idle scan, `!mission` so The Relic War stays fair) carry loose relics to their monastery; carrying beats healing/converting.
  - **AI town defense** (CPSB): villagers within 8 of the TC garrison inside when enemy military is within 8 (`ai.lastDanger`), eject ~10s after quiet. Villagers add TC arrows via the existing garrison-arrow rule.
  - Tested: Hallowed 3p Turbo Warlord 8-min (AI enshrined 6/8 relics, 0 errors), TC garrison in/out (10 in, 0 after clear), save/load turbo+relics, River regression 10-min 0 errors.
- Label "art-fidelity-pass" (2026-08-05, sw tq-v8, commit e57e31a): user asked the graphics to match the real game. All ORIGINAL art, palettes re-tuned by eye against the reference frames (`f600.jpg`/`f1500.jpg`, still in the 03f3d2c7… scratchpad; observations in AOE2-REFERENCE.md). Changes: GR/GRH grass → brighter yellow-olive (#7e8946 family; GRH stays within ~+6%, gotcha respected); grass macro patches bigger (mr 2.0–4.8 TILE) with FAINT solid cores (.11/.08 alpha) + heavy dither (34 specks/stamp) — **solid-ellipse chains read as stamped blobs, dither carries the shape**; DIRT warmer orange-tan (#c0a266…); TERRA deepened to brick (#9a5a3e); HTHATCH → grey-straw; water bluer/deeper (#2c567f mid, #41749e edge); broadleaf canopy palettes darkened (forest reads near-black in mass); OUT → rgba(50,33,17,.72) (crisper sprites, still warm — supersedes the old "don't darken outlines" note but stays far from pure black). Fog alphas UNTOUCHED (gotcha #4). Verified via /save captures (artpass-town/water/final.jpg beside serve2) + 5-min sim 0 errors, draw 8.6ms revealed at z1.15.
- **This update deliberately updated the house style spec**: the "prerendered-art-pass" palette notes above are now historical where they conflict with this label.
- Label "click-offset-fix" (2026-08-05, sw tq-v7, commit a1b25cd): **user-reported bug — taps landed ~an inch from the cursor.** Root cause: `resize()` only ran on window resize, but the bottom panel's height changes with every selection (tech buttons are full-width 32px rows), shrinking `#viewwrap` with NO window event → stale `vw/vh` → canvas drawn squashed → tap math offset (worst near screen bottom, scale error cssH/vh). Fix: `chkView()` (compares wrap.clientWidth/Height to vw/vh, calls resize()) wired to BOTH a ResizeObserver on #viewwrap AND a 20-frame periodic check in `frame()` (observer callbacks don't fire in hidden/background tabs, so belt-and-braces). Verified headlessly: panel grow 557→519 cssH, chkView() re-syncs, tap round-trip error 0px. **Gotcha: any future layout change that can alter #viewwrap's size without a window resize is already covered by chkView — don't add per-feature resize calls.**
- **IP boundary (2026-08-05, again)**: user asked to swap in the real Microsoft art from the CD ("I have permission"). DECLINED — the game is publicly hosted (Render + artifact); shipping Microsoft's SLP sprites/sounds is redistribution regardless of a personal-permission claim we can't verify. Standing offer instead: targeted original-art fidelity passes (user says current graphics "don't match the game" — ask WHICH pieces look off and redraw them closer to the AoE2 look).
- Label "cd-update" (2026-08-05): user supplied the FULL Conquerors CD contents at `C:\Users\deove\OneDrive\Documents\AOE2CONQ\` (4 language dirs; UK = English). Extracted data (facts only, no MS assets): TECHTREE.PDF (per-civ team bonuses — mirrored text, reverse each word), MANUAL.PDF UK (stat/cost tables pp. 42-49), SAMPLEAI.PER (AI parameters), WHATSNEW.RTF. All distilled into **AOE2-REFERENCE.md § CD DATA** — read that before adding remaining content. This update shipped essentially the whole remaining queue:
  - **Siege engines** (siege workshop; `siege:true` flag): scorpion line (`pierce` — bolts damage enemies along the flight line), mangonel line (`blast:1.6` splash, also around bombarded buildings), **trebuchet** (castle-trained at Imperial, castle key T; `treb:true`; 3s unpack via `u.setup` — cleared by cmdMove/attack-chase; 12 dmg vs units), **bombard cannon** (`gun:true,chem:true`). Siege/gun EXCLUDED from Fletching atk/range and from ranged-vs-ram cap + arrowRes via `dealDamage(..., 'siege')` (checks are now `ranged===true`). Projectile `kind`: 'stone' (high arc + dust/chip impact), 'ball' (flat, muzzle smoke), 'bolt'. Rigs: drawScorpion/drawMangonel (animated arm)/drawTreb (diagonal-snapped, packed-vs-set-up pose)/drawBombard; handcannon = MAN_COSTUME 'gun' weapon branch (kick + muzzle flash).
  - **University** (age 2, 200W, key U) + `UNI_TECHS`/`G.P[p].uni{}`: mas/arch (+10% bld HP each, retroactive via applyUni + addBld mult), tread (+20% build rate), **bal** (Ballistics — `missChance(p)` .28→.08; tower + unit shots), heat (towers ×1.5 vs ships), **chem** (gates `chem:true` units via trainsFor filter; +1 missile atk except gun), se (Siege Engineers: +1 rng, +20% vs bld, petard +40%), **btower** (towers +6 atk, cannonball art, 'siege' damage). ISO_ART.university (slate roof + observatory dome).
  - **Gunpowder**: hand cannoneer (range, key H), cannon galleon (dock, key N; drawShip `cannon:true` param), bombard tower via btower tech. Turk gunHp now covers all `gun` units.
  - **ECO_TECHS** (generic per-building research buttons rendered in refreshPanel building branch; state `G.P[p].eco{}` counters; `applyEco` retroactive): TC loom/cart(Wheelbarrow,Hand Cart)/watch(Town Watch,Patrol → bld LOS in updateFog); camp wood(3 tiers → WOOD_RATE)/gold(2 → GOLD_RATE)/farm(Horse Collar/Heavy Plow/Crop Rotation → farmFoodOf); market caravan; dock careen/dry/shipw + **Fish Trap button** (100W → spawns a 250-food fish res on open water near the dock); stable bloodlines/husb; barracks track/squires; range thumb (rof ×.85 + no miss); monastery ferv/sanc/red/aton/her/herb/illum/faith/bp.
  - **Farms expire** (manual): `b.food` seeded lazily from farmFoodOf(p) (175 + tiers + Chinese TB 45), −1 per gather tick, auto-replant costs a farm (60W) when it runs dry, else farm dies ("gone fallow" feed). Works for AI too (they pay their own wood).
  - **Monk conversion**: cmdAttack intercepts for monks → cmdConvert; states toConv/convert; `convRange(p)`=5+2·bp; `convTimeFor` 5s units/8s blds ×1.5 scout-line ×1.5 Faith ×1.6 Teuton TB; faith rest 30s (15 w/ illum) via `u.faithReady`; Heresy kills victim instead; Redemption converts blds (not tc/castle/walls/farm/monastery/wonder — ejects garrison, clears queue) + siege; Atonement converts monks. AI monks convert when idle & no one to heal; AI monk count [1,2,4] by difficulty.
  - **Team bonuses** (`TEAM_BONUS` map civ-id→key, `teamHas(p,key)` scans allies incl. self): britons rangeFast/celts siegeFast/goths barracksFast/huns stableFast/turks gunFast (trainTimeOf ×.8); byz healFast (9hp/s); chinese farmFood; franks knightLos/mongols scoutLos/japanese galleyLos (updateFog); koreans mangonelRng (+1); mayans wallCheap/vikings dockCheap (bldCostOf); persians knightVsArcher/saracens archVsBld (attack case); spanish tradeGold; aztecs relicGold (+33% relic trickle); teutons convResist.
  - **Wonder** (age 3, 1000W+1000G, bt 100, key N; ISO_ART gilded dome) → `wonderRaised(b)` on completion → `G.wonderT={p,id,end}` 300s (+100 if anyone has Atheism); countdown in #ageLbl (🏯 / ⚠️🏯); cancels with feed when razed; win/lose via endGame(allied(p,0)). **Relic victory**: all relics enshrined by one team → `G.relicT` 300s countdown (✨). Both skirmish-only (!mission), both SAVED (wonderT/relicT in save v1 + loader). AI: builds wonder when very rich at Imperial; `nearestEnemyBld` targets an enemy wonder above all else.
  - **AI (SAMPLEAI.PER)**: gatherer split by age (Dark 60F/40W; Feudal 35F/25W/40G; Castle+ 30/30/40), builds university+monastery at Castle Age, researches uni techs (chem first) + cheapest eco tech when rich, trains scorpion/mangonel/bombard (≤2 each), trebs ≤2, monks by difficulty.
  - **Gotchas**: `teamHas` is used inside rangeOf/addBld etc. — it's defined ABOVE civOf next to CIVS and guards `!G`. New-session artifact republish now requires WebFetch-ing the artifact URL first (freshness check) — do that before Artifact({url}). Panels: eco buttons render for ANY building match, so adding a tech with `bld:'X'` auto-appears when an X is selected. `u.setup` (treb) must be cleared on movement or it fires instantly after redeploying.
  - Tested: all unit stats/lines/gates headless; mangonel splash / scorpion pierce / treb setup+chunks; masonry retroactivity; chem gating; ballistics; loom/cart/bloodlines/husb/thumb; farm 550-food w/ Crop Rotation + auto-replant (60W observed); fish trap +146 food/2min; conversion incl. faith rest, Heresy, Redemption gate; wonder countdown + cancel-on-raze + win; save/load round-trip of uni/eco/farm.food/treb/wonderT; 24-min Warlord sim (AI Castle Age, university techs, mangonels+rams+monks, 8 replanting farms, 0 errors); 8p/96 Black Forest 2-team sim 0 errors; every building/unit panel branch exercised, 0 errors. sw tq-v6, commit 01af434.
- Label "navy" (2026-08-05): Docks + 8 ship types (manual p. 44).
  - **Naval pathfinding**: `naval` flag threaded through findPath/walkTile/losBlocked/lineClear/hasLOS/smoothPath/nearFree; `passableW(x,y)` = water||ford minus `G.navBlock` (dock footprints). ALL unit repaths route through `uPath(u,tx,ty)` (ships auto-naval). **GOTCHA: a global string-replace of findPath(u.x,u.y,→uPath(u, also rewrote uPath's own definition into infinite recursion — check the wrapper after any such sweep.** followPath: ships use passableW for waypoint/walk checks; motParams ship = slow wide arcs.
  - **Dock** (BLDS.dock, key D, drop:true, trains 6 ship types + civ uuShip): placement via `canPlaceDock` — 2x2 open water (never fords), touching land, AND a local flood-fill proves it doesn't dam the channel (a west-edge dock stranded ships in a 9-tile pocket before this rule). Docks keep G.map=1 on removal (water stays land-blocked) and clear navBlock. No stampWorn on docks. Ships spawn via nearFree(...,naval).
  - **Ships**: fishing (fisher, drops 'fish' carry→food at dock only; carryCap 15; autoNext chains schools), cog (idle → auto trade route between own dock and nearest foreign dock, gold=8+dist*1.2 per round trip), transport (garCap 5, boards via generalized cmdGarrisonRam — any land unit, reach 2.1; unload button drops on nearFree land; sinks drown passengers if no shore), galley line (Galley/War Galley/Galleon by age), fireship (rof .4 vsShip), demo (area blast 110/.6 within 1.9, self-destructs), Viking longboat + Korean turtle (uuShip, elite at Imperial). Viking shipCost .8, Japanese fishHp 2.
  - **Fish** res type in the river (genMapRiver widened to 4 tiles; every ~6th column); fish DON'T reset G.map on depletion; minimap pale-blue dots; getResSpr 'fish' ripples+leaping fish. **GOTCHA: cmdGather gate must coerce `!!fisher!==(r.type==='fish')` — comparing undefined!==false sent every villager walking instead of gathering and silently killed ALL economies.**
  - AI: dock near the closest waterList tile on a 25s retry cooldown (bases are ~30 tiles from the river — a TC-radius scan never finds it); trains ≤2 fishers + ≤3 warships; idle fishers auto-fish.
  - Art: ISO_ART.dock (pile pier + boathouse + crane; NO dirtPad), drawShip shared painter (diagonal-snapped like the war wagon; team-striped sails, oars, dragon prows, turtle carapace, brazier, powder kegs), bob + wake.
  - Tested: fishing loop +194 food, cross-water galley kill, trade route +58g, transport board/unload, demo blast, turtle 300hp elite, save/load navy + navBlock rebuild, Warlord AI navy (dock+2 fishers+3 galleys), BF no-water regression, 8p. sw tq-v5.
- Label "blacksmith" (2026-08-05): Blacksmith building + Parthian Tactics (manual pp. 45-47).
  - **BLDS.blacksmith** (Feudal, 150W, size 2, key S). **GOTCHA: every new building type MUST get an `IHT` entry** (iso sprite height) — missing it makes mkIsoSpr build a NaN→0-size canvas and portraitFor throws on drawImage.
  - **BS_TECHS** 5 lines × 3 tiers (tier gates age II/III/IV): atk Forging/Iron Casting/Blast Furnace (+1/+1/+2 melee via BS_ATK in atkOf), arw Fletching/Bodkin/Bracer (+1 atk +1 rng per tier for ranged units; buildings +1 atk +0.5 rng per tier), ia/aa/ca armor lines = FLAT damage reduction per hit (no armor stat in engine) applied in dealDamage via bsArmorOf, min 1 dmg. State G.P[p].bs={atk,arw,ia,aa,ca} + G.P[p].pt (Parthian) — mkP seeds, loader defaults.
  - **Parthian Tactics** at Archery Range (Imperial, 200F 250G): mounted archers (cav+ranged) take -2 extra.
  - AI: builds smithy at Feudal (w>=250), buys cheapest affordable tier when rich, Parthian at Imperial.
  - Art: ISO_ART.blacksmith — stone shop, terracotta roof, chimney, plank lean-to porch w/ glowing forge + anvil + horseshoes.
  - Tested: all tier effects (17 atk champion w/ Blast Furnace, 9/9 arbalest, armor -3, Parthian -5 total), panel buttons, save/load bs/pt, 12-min AI sim (AI researched 4 tiers), zero errors. sw tq-v4.
- Label "conquerors-manual" (2026-08-05): user supplied the real AoE2:The Conquerors manual PDF (`C:\Users\deove\Downloads\Manual_AOE2_CONQ.pdf`, 51 pp; full text extracted — re-extract with pdfplumber, Python 3.14 IS now installed, the old "Store stub" note is dead). "Rewrite everything based off of this manual" → full content overhaul:
  - **18 civs** (manual pp. 27-29): first 12 in FROZEN order (save compat), then japanese/aztecs/mayans/huns/koreans/spanish. Each has manual bonuses adapted (no navy/blacksmith/stone) + `ut` unique tech id. New civ hook fields: infSpd, vilSpd/vilCarry, castleCost/towerCost/campCost, counterCost/infCost/archCost, impDisc, techDisc[], bldHpAge[]/infHpAge[]/infRofAge[], tcHp, startF/startW, scoutHp, gunHp, mangRof, siegeRof, garArrow/towerGar, healRange, towerRangeAge[], vilLos, resLast, mktRate, buildFast, monkHp, popBonus, noHouse, noStable, eagles, missionaries.
  - **Unit stats = manual tables** (pp. 42-44). `line:{name,hp,atk,rng}[age]` for upgrade lines (Militia→Champion, Spearman→Halberdier, Archer→Arbalest, Scout→Hussar, Knight→Cavalier, Ram→Capped Ram, Skirm→Elite); UUs get `elite:{...}` stats at Imperial. `statFor(type,p)` is the single stat resolver (addUnit/atkOf/rangeOf/unitName). Generic +age scaling REMOVED. rofOf/rangeOf helpers. NEW UNITS: eagle, jaguar, plumed, warwagon, conquistador, tarkan, samurai (vsUnique via UU_SET), petard (explodes, 60 vs blds), missionary (fast mounted monk, noRelic). berserker renamed 'Berserk' + regen (0.4hp/s, 1.0 w/ Berserkergang).
  - **Unique techs** UTECHS (manual pp. 21-25, costs pp. 47, stone→wood/gold): castle panel button (human), AI researches when rich; goths get anarchy(Castle)→perfusion(Imperial). G.P[p].uts[] (save v1-compatible; loader defaults []). applyUt does retroactive HP bumps.
  - **Manual mechanics**: infantry garrison INSIDE rams (u.gar on ram unit; tap ram w/ infantry selected; +8%speed/+5atk each; eject on ram death + panel button; save strips nested paths), smarter villagers (finished camp → builder auto-gathers nearest res), Huns skip houses (popCap=POP_MAX), Teuton monk heal range ×2 / tower garrison ×2 / garrison arrows ×2, Korean vilLos in updateFog, Atheism = spy vision (enemy units cast fog for p0), Saracen market 95/105 rates, Chinese/Byz age discounts via ageCostOf, unit/bld cost discounts via unitCostOf/bldCostOf (wired into panel+hotkeys+AI+aiPlace).
  - **Art**: MAN_COSTUME samurai/jaguar/eagle/plumed/petard (+helm 'jaguar','plume'; mc.spots; weapon 'bomb'), CAV_COSTUME conquistador(gun+muzzle flash)/tarkan(torch)/missionary(cowl, no weapon), drawWarWagon custom rig (heading SNAPS to diagonals — end-on it collapsed to a bundle otherwise).
  - **Gotchas**: saveGame() returns false when G.over — a 10-min unattended sim ends in a loss, so test save/load on a LIVE game. mkP() in newGame applies startF/startW. trainsFor(b,p) is the single source of trainable units (castle=[uu,petard]) — panel, hotkeys, AI all use it.
  - Tested: 18×(90s sim + imperial + tech research + UU combat) zero errors; ram garrison/eject; petard; berserk regen; line stats; save/load round-trip incl. ram passengers; 8p Black Forest w/ new civs; Huns built zero houses. Deployed sw tq-v3.
- Label "prerendered-art-pass" (2026-08-05): user supplied a full art-direction spec — "late-1990s pre-rendered isometric RTS" look. **This is now the house style; keep new art consistent with it:**
  - Warm key light upper-left, soft COOL ambient fill on shaded faces (isoBlock + roofGable overlay cool `rgba(46,54,74)` on east faces, warm `rgba(255,224,166)` on west), baked soft cool drop shadows (multi-pass ellipses in dirtPad/drawUnit/groundShadow).
  - **NO black outlines** — `OUT` is now `rgba(64,44,26,.62)` (warm soft), all `rgba(14,9,4,…)` limb/leg underlays became `rgba(64,44,26,.5)`; volShade adds a clipped pale rim stroke. Don't reintroduce near-black outlines.
  - Palette: terracotta roofs (`TERRA` const; tc/barracks/stable), whitewashed plaster (CREAMW brightened), slate STONE (cooled r face), slate monastery roof; `archWin()` helper for round-arched doors/windows (tc, barracks, house, monastery).
  - Barracks REDESIGNED: stone plinth + plaster/timber + terracotta gable (was flat-top stone keep with merlons).
  - Terrain GR/GRH muted/desaturated dry-grass olives; speckle + grain alphas reduced.
  - UI: carved gray stone chrome (topbar/panel/.cmd/zoom), bronze rivets + outlines `rgba(199,141,66)` / `rgba(214,152,74)`, torchlit warm radial on panel; parchment + gold kept.
  - Team colors deliberately KEPT high-chroma (readability trumps the spec's "muted" note).
  - **Testing infra**: `serve2.ps1` (port param, default 8742, plus **POST /save** — page POSTs base64 → file beside server; used to capture canvas screenshots when the Browser pane is hidden and `computer screenshot` times out). `.claude/launch.json` (in `C:\Users\deove\.claude\.claude\`) has a `tiny-conquerors` entry for preview_start. Gallery-scene + camera math: `cam` is a screen-space top-left — center via `p=isoPt(tx,ty); cam.x=p.x-vw/z/2`. Gallery buildings need `queue:[],qt:0,gar:[]` or drawBld throws.
  - Verified: gallery + unit closeups via /save captures, UI computed styles, 10-min sim + BF start, zero console errors. Deployed: sw `tq-v2`, commit 9810717, Render live-verified, artifact republished.
- Label "sound-ui-polish" (2026-08-04): full WebAudio soundscape rebuild + UI fidelity pass.
  - **Sound**: master bus now has a DynamicsCompressor; new synth helpers sKnock (woody pitch-drop knock), sMetal (inharmonic partial-stack clang), sWhoosh (swept bandpass air), sBrass (detuned saw stack through opening lowpass), sVoice (formant grunt), sBird (ambient chirps on a 9s interval timer started in initAudio, gated by mute/pause). Redesigned every kind: chop=crack+woody body, mine=tink+gravel, sword=real clang+whoosh, hit=thud, click=wooden tock, done=hammer×2+chime, age=timpani+brass fanfare, horn=two-blast war horn w/ minor-third dip, win/lose=brass fanfare/dirge. NEW kinds: 'ack' ("hup" on every issued order — wired via flashSel()), 'sel' (higher "hm?" on selecting own unit), 'place' (building placement thud; wall line too).
  - **UI**: selection ellipses now WHITE (AoE2-authentic; inspect = dimmer white), custom ivory SVG arrow cursor everywhere, .cmd buttons got hard 2-tone bevels + gold hover + pressed inset, ICO.crown SVG replaces 👑 on age-up, ICO.atk on Attack-move.
- Label "aoe2-total-overhaul" (2026-08-04): TOTAL visual overhaul to match the user's AoE2:TC video (https://www.youtube.com/watch?v=ccN33zXNZSQ — re-watched, 7 frames captured; observations in AOE2-REFERENCE.md). Built via ultracode workflows: 6 spec agents → implementation → 26-agent adversarial review (22 confirmed findings, all fixed). Zero console errors; full regression suite passed (10-min sims, save/load, 8p/96 BF, M5, all input paths).
  - **Terrain**: walked dirt/sand macro-blob fields (dirtSpots/dirtPatches feed bones + minimap), dark/pale grass macro walks, SOLID forest understory (near-black depth-graded fill + scalloped canopy fringe; `stampCleared(x,y)` repaints chopped tiles — MANDATORY pair with the understory), doodads (two-tone tufts, bushes, rock clusters, fallen logs, ribcage/bone decals in dirt), banks skip sand under wood.
  - **Fog/minimap**: unexplored hue near-black (3,4,7) but ALPHA STAYS 208 (gotcha #4 — review caught a 244 regression, restored); mist neutral gray @ .35; minimap canvas now 200x116, diamond-clipped w/ stroked gold frame + corner studs, transforms a=96/MAP,b2=48/MAP origin (100,10) in drawMini AND tap handler (corner taps outside diamond ignored); pure-black unexplored via maskC blit; own units WHITE dots; attack pings (G.pings, dealDamage + aiThink wave hook). Backdrop + draw() clear are near-black #12160b.
  - **Aftermath**: corpses 150s staged decay (fresh/dark/skeleton, CORPSE_SPR cache, ram→wreck sprite, cap 140, h flip); rubble piles on building death (G.rubble, RUB_SPR via mkIsoSpr, 45s fade, dust burst via tintable smoke f.col); arrows miss 28% vs units → stick in ground (G.stuck, 8s); higher arrow arcs + motion trails; death splinter burst. G.rubble/G.stuck/G.pings in BOTH state literals (newGame+loadGame), never serialized.
  - **Sprites**: TEAMS now high-chroma AoE2 colors; OUT/limb/legs/horse-leg outlines hardened to rgba(14,9,4,.8-.9); O2 .62; dirtPad two-pass long shadow; houses age-keyed (thatch <Castle, then per-instance clay/timber/slate via HROOF/HTHATCH; getBldSpr key now type_p_built_stage_ab_vr, vr only for houses); ivyAt() vines on tc/tower/castle/monastery; knights get full draped caparison (cataphract keeps small rect).
  - **Top HUD/overlays**: thin dark topbar (centered #ageLbl ≥660px), .ovt overlay layer on #viewwrap: #clock (game time + difficulty/mission), #feed (event log: X Created / — Built — / age / RED warnings incl. "attacked by the <civ>"), #ticker (#army counts + per-player #score in team colors, capped 5 lines on phones). feed()/updateOverlays() (1/sec throttle via ovT)/updateArmyCounts/updateScores near toast(). begin() clears feed + ovT.
  - **Bottom HUD (AoE2 4-zone)**: #panel CSS grid "cmd card scroll mini" (phone: 2-col, miniDock hidden); #btnRow = 46px square portrait tiles (5-col, w:266px for scrollbar), .txt/.go/.warn full-row banners; #unitCard (name/portrait/HP bar/stat rows w/ inline-SVG ICO); #scrollArea parchment (civLine, selGrid multi-select portrait tiles ≤24 w/ tap-to-pick + HP slivers, scrollHint, watermark); portraitFor(kind,type,p) renders via GLOBAL `let ctx` swap + drawUnitBody (extracted dispatch) into 88px dataURLs, PORTRAITS cache keyed w/ age-bucket for 'b' + militia age; mkBtn(html,cls,fn,dis,icon) + parchment hover tooltips; updateCard (.3s HP patcher, skips placement, rebuilds when tile count ≠ ents) + updateInspect (.5s in-place inspect patcher — NEVER full rebuild, fog-gates enemy HP); placeMini() docks minimap+zoomBtns into panel ≥760px (matchMedia + resize fallback — mq change event alone missed emulated resizes).
  - Touch hit radius: `hd=(mouse?13:22)/Math.min(1,G.cam.z)` — review caught the screen-space rewrite shrinking touch targets 4x.
  - Esc also clears G.inspect. Toast z-index 6 (above overlays/minimap).
- Label "click-fix" (2026-08-04): screen-space hit tests in handleTap — clicking the visible ART (canopy above its tile, roofs above footprints) selects the thing; visual res pick reassigns res/resKey (front-most rp.y wins); building roof-box fallback via IHT heights.
- Label "aoe2-controls" (2026-08-04): AoE2-style selection & mouse commands (user complaint: couldn't select trees/gold, double-click sent villagers to build, had to deselect before selecting something else).
  - `handleTap(cx,cy,mouse,right)` — endPtr passes `p.mouse` (pointerType==='mouse') and `p.btn===2`; ptrs entries store btn/mouse; `contextmenu` preventDefault on view.
  - **Mouse:** left-click ONLY selects (command block gated by `haveSel&&(right||!mouse||G.amode)` — amode tap is the exception); right-click issues all commands (gather/build/attack/garrison/farm/relic/move) and never changes selection (`if(right)return` before selection); right-click also cancels placement mode. Touch behavior unchanged.
  - **Inspect selection** `G.inspect` (`{res:key}` or `{id}`): left-click/tap with the click resolving to no own thing selects resources (tileKnown) or enemy/ally units+blds. Panel (refreshPanel empty branch) shows 🌲/🪙/🍓 name + amount left, or Ally/Enemy name + HP; live-refreshed every .5s from top of step(); auto-clears when depleted/dead or when G.sel becomes non-empty (`if(ents.length)G.inspect=null`). White rings (vs gold for G.sel) in drawUnit/drawBld/res branch of the ents loop (`inspR` computed before loop). `mouseSeen` global tailors hint texts to "right-click" wording.
  - Headless-tested: 13 scenarios (inspect tree/gold/enemy, left-vs-right gather/move/build/garrison/attack, touch path, dbl-click select-all, amode left-click, placement cancel, live panel update + depletion clear, 200s sim) — zero console errors.
- Label "big-battles" (2026-08-04): up to 8 players + alliances + dynamic map size.
  - **MAP is now `let`** (64 for ≤3 players, 96 for 4+, via `setMapSize(n)` in the rendering section — resizes terrain/fogC/maskC/miniResC and nulls grainPat). `TREZT` (terrain supersample) drops 2→1 on 96 maps to cap terrain canvas memory (~25MB); sprites keep TREZ=2. `START` is `let`, built by `genStarts(n)` — ring at r=.4*MAP, player 0 SW (angle 3π/4).
  - **8 team colors** (AoE2 order-ish: blue you, red, green, yellow, cyan, purple, gray, orange). Start screen: data-n 2..8 players row + data-t teams row (`playersSel`,`teamSel` replaced npSel).
  - **Alliances**: `G.teams[]` + `allied(a,b)` (near civOf). Modes: FFA / Two teams (first half incl. you) / You-vs-world. Applied everywhere via sed: nearestEnemy, handleTap attack+visibility, draw/minimap visibility, updateFog (ALLIED SHARED VISION — allied units/blds cast player fog), A* pass2/stepCost (allied gates passable, allied walls block, only enemy walls +30), followPath wall-bash (won't attack allied walls), gate-open check, win condition (`!allied(b.p,0)` TCs — win even if ally's TC stands), monk heals allies.
  - **AI targeting**: nearestPlayerThing → `nearestEnemyBld(fromB,p)`; ai.tgtId stores wave target; wave toast only when target is you (or "ally under attack"); aiJoinDefense/idle-regroup use tgtId.
  - Missions: M5 Two Fronts now `vsWorld:true` (allied warlords, 64 map). Save v1 adds mapN/starts/teams (loader defaults old saves).
  - Tested: 8p FFA 96-map (8 TCs, min start dist 29, cross-map path ok, all AIs alive, sim 2.4ms/step), 4v4 (shared vision ✓, ally not targetable ✓, win with ally alive ✓), 1v4 vs-world, BF 96 (5.8k trees, 8 clearings+spokes), save/load big map, M5, 2p/64 regression. Zero console errors.
- Label "aoe2-ground" (2026-08-04): user linked an AoE2:TC video ("1 vs 6 hard computer") asking to match it. Diagnosis: the last "browser-game tell" was the per-tile checkerboard. Fix in refreshTerrain: GR/GRH palettes collapsed to tightly-spaced warm olives (checker invisible; hills only +6% w/ rim lines carrying the read), and a `grainPat` canvas pattern (96px, 1100 seeded dark/light specks, cached in the `grainPat` global next to lastTreeCount) tiled over the WHOLE ground right after the base tile loop — that grain, not tile contrast, provides the texture, exactly like AoE2's dithered terrain. Macro blobs warmed slightly. Gotcha: first attempt left hills as sharp pale geometric patches — GRH must stay within ~+6% of GR.
- NOTE: the 1v6 video setup is now fully supported (big-battles allows up to 8 players / 96-tile maps). Video URL: https://www.youtube.com/watch?v=ccN33zXNZSQ; style observations in AOE2-REFERENCE.md.
- Label "ui-polish" (2026-08-04): UI/buttons pass, pure CSS + icon injection. Top bar & panel: carved-wood gradients, inset gold-tinted top highlight, drop shadows; resource chips are recessed (inset shadow). `.cmd` buttons: wood gradient, bevel (inset top highlight + outer shadow), :active pressed state (translateY+inset), emoji icon row via `<i>` (block, 16px, drop-shadow); `.cmd.go` gets gold border+glow. `BLD_ICONS`/`UNIT_ICONS` maps next to BUILD_KEYS; icons injected in refreshPanel (build list, train list, age-up 👑, misc buttons, market trades). #btnRow capped at 150px with overflow-y auto (15 build buttons!). Overlays: parchment gradient + inset gold outline ring; .bigbtn gradient+press; minimap gold outline; toast/zoom/diff .sel polished. Verified via DOM (15 iconed build buttons, computed styles applied).
- Label "building-beauty" (2026-08-04): building fidelity via the SHARED helpers (all bld sprites benefit at once, ISPR cache rebuilds per load). `masonry()` now draws individual stone quads: staggered courses, 5 hash-varied tints, bevel highlight on each stone's top edge, dark bed/head joints, mossy stones on the ground course (hsh%6). `roofLines()` → shingle rows: alternating row tint quads, per-shingle joint ticks (8/row staggered), one bleached/dark shingle per row, eave drop-shadow, lit ridge. `isoBlock()` adds a wide soft AO band above the ground line + sunlit top-edge stroke. `bannerAt()` → waving swallowtail pennant with wind-lit fold + gold finial. `faceBeams()` adds plaster stipple + beam drop-shadows. Market got plank decking on its rooftop terrace.
- Label "soft-fog" (2026-08-04): fog of war overhaul. fogC (64px, hard cells — still used by minimap) now upscales through fogM(128)→fogS(512) with bilinear smoothing → soft organic cloud edges; draw() blits fogS at MAP units inside the iso transform. Fog hue shifted to blue-slate (unexplored 8,12,24 α208; explored 12,16,28 α84 — SAME alphas as the visibility-fix gotcha, only hue changed). Drifting mist: seamless noiseC (256, wrapped radial blobs, mulberry(4242)) composited 'source-in' against maskS (512 upscale of unexplored-only maskC) every frame, drawn at globalAlpha .6; drift speed tied to G.t (4.5,2.6 px/s). Perf: ~2.1ms/frame total at z=1 near base. All canvases declared right above renderFogCanvas.
- Label "resource-art" (2026-08-04): berries + gold rewritten in getResSpr. Berry bush: woody stems, base shadow-mass ellipse + 6 leaf clumps in 4 rising tones + shade dapples + 4 berry clusters (3 berries each, dark rim/red body/pink eye), soft outline. Gold: `crag()` helper (sunlit west face / shaded east face / ridge highlight) ×3 crags, gold vein strokes in the cracks, `nug()` nuggets (dark rim/gold/pale facet/white glint) ×6, rock debris at base, star sparkle. Seeded via mulberry per variant (h%8 key from tree pass applies).
- Label "water-pass" (2026-08-04): river/water overhaul in refreshTerrain + draw. Water tiles are depth-graded (edge tiles lighter teal via `isLand` 4-neighbor check, mid-river deep blue, per-tile linearGradient), with baked double wave glints, submerged pale stones on edge tiles, and foam strokes along every water↔land edge. Banks: dry sand (8px) over wet sand (4px) strips + sand speckles dithered into grass + reed clumps w/ cattails on hh%4 tiles. Fords: wet-sand gradient, two blue ripple bands, animated-look sheen stroke, 3 stepping stones with wet highlights. Animated layer (draw loop): original drifting arc + second slower counter-drifting ripple + sun-glint pixels when sin phase >.92.
- Label "detail-art-pass" (2026-08-04): user asked for "actual bodies and actual trees, beautiful and detailed".
  - **Trees rewritten** (getResSpr wood branch, key now h%8, seeded via mulberry(h...)): broadleaf = trunk with root flare/fork/bark grain + canopy of ~11 leaf clumps in 6 rising tones + sunlit speckles + SE dapples; pine = 4 drooping frond tiers with needle streaks and lit west edges.
  - **Unit anatomy helpers** near limb(): `volShade(pathFn)` (clip + darken right flank/warm left — the key volume trick), `hand(x,y)`, `faceEye(hx,hy,r)` (cheek shade + eye dot); legs() now bend at the knee (quadratic). All infantry draw fns use body-path closures + volShade; villager has sleeves/hands/straw cap; militia has mail rings, pauldron-free but gleaming helm w/ nose guard, sword with fuller, shield boss; spearman has quilted jack + two hands on spear + lit kettle brim (woad: warpaint chest, topknot, cheek stripe); archer has quiver w/ fletching, recurve bow w/ wrapped grip + both hands + nocked arrow when attacking. drawHorse body is shaded (belly shadow/haunch highlights) + bridle/rein/nostril; knight rider cuirass shaded.
  - **Perf**: tree sway is now adaptive — `lastTreeCount` global; sway only when z>=.9 AND <320 trees drawn last frame (dense revealed BF at z=1 was 10.7ms, now 7.5ms with sway auto-off). Also draw() guards vw/vh==0 and 0-size vig (hidden-pane crash).
- Label "big-update" (2026-08-04): the whole remaining queue, all headless-tested, zero console errors.
  - **Stone walls/gates**: BLDS swall(1500hp)/sgate(2000hp), age 2, thin/gate flags; wall LINE placement generalized (all `G.placing==='wall'` checks are now `==='wall'||==='swall'`, canPlaceType/addBld use G.placing, cost check uses canAfford not just wood). Keys X/P.
  - **Market** (key K, age 1): panel trade buttons sell 100F/W→70G, buy 100F/W←80G.
  - **Monastery/monks/relics**: monastery (key M, age 2) trains monk (atk 0, `monk:true`). Monk idle-scans wounded allies → states toHeal/heal (+6hp/s within 2.2). `G.relics[]` ({id,x,y,held,mon}) placed by `placeRelics()` after genMap (4/map). Tap relic with monk → toRelic → carries (u.relic); tap own monastery → toMon → deposits (r.mon=bldId, +0.55g/s in step). Relic drops on monk death; pops out in removeBld. Drawn via getRelicSpr billboard when !held&&!mon&&tileKnown; gold dots on minimap.
  - **Control groups**: Ctrl+1-9 assign, digit recall, repeat-press centers. `G.groups` (saved).
  - **4 new civs**: goths(infHp 1.1, trainFast .75)/chinese(startVills 1, farmFast)/persians(bldHp+cavHp 1.15)/turks(goldMult 1.2, towerAtk 1.25). New UNITS: huskarl(`arrowRes` → ranged dmg halved in dealDamage), chukonu(`rof:1.0` — attack cd is now `d.rof||1.5`), elephant(new drawElephant art), janissary(rof 2.0). New civ hooks: trainFast (queue timing), startVills (newGame).
  - **Second AI**: `NP` global (players incl. you), `npSel` UI (`data-f` buttons). START has 3rd entry [10,10] (works both maps; BF gets a 3rd trail). G.ais array replaces G.ai (aiThink(p,ai), aiPlace(...,p)); all `p===1` render/fog/win checks generalized to `p!==0`/`p>0`; TEAMS[2]=purple. Win = ALL enemy TCs razed.
  - **Missions 4-5**: The Relic War (win type 'relics' n:3, prebuilt monastery+monk, checked in step) and Two Fronts (`foes:2` → NP=3).
  - Save v1 extended: np/ais/relics/groups (loader tolerates older saves without them).
  - Gotcha fixed: draw() now no-ops when vw/vh are 0 (hidden pane crashed drawImage on the 0-size vignette canvas).
- Label "gameplay-update" (2026-08-04): four features shipped, all headless-tested.
  - **Garrison**: `garCap` on tc(10)/castle(10)/tower(4); `b.gar[]` on every bld; tap own garrisonable bld with units selected → `cmdGarrison` → state 'toGar' → sets `u._gar`, transfer finalized AFTER the unit loop in step() (never splice mid-iteration). +2 arrow dmg per garrisoned unit; Ungarrison btn on panel; `ejectGarrison(b)` also runs in removeBld; popUsed counts gar.
  - **Attack-move & formations**: `G.amode` toggle btn (military panel) → next ground tap issues `cmdAmove` (state 'amove', scans enemies every .4s within 4.5, engages preserving `u.am`, attack case resumes amove when target dies; cmdMove/cmdAttack clear `u.am`). `groupDest(n,i,tx,ty)` spreads group moves into a grid (used for both move & amove).
  - **Save/load**: `saveGame()/loadGame()` (localStorage 'tq_save', v:1) — strips paths, serializes gar arrays, elev/vis as plain arrays, rebuilds wallMap/gateMap. 💾 btn in pause menu; ▶ Continue btn on start screen (`updateContBtn()`).
  - **Campaign**: `MISSIONS[3]` + `let mission` global. M1 First Blood (raze, +4 militia), M2 Hold the Fords (survive 600s, Feudal start, countdown shown in #ageLbl via updateTop), M3 Black Forest Gambit (raze, AI starts Castle Age + small army). Progress in localStorage 'tq_camp'; locks in showCampaign(); mission.setup() runs in begin() after newGame; survive-win check at top of step()'s win block; endGame writes progress + mission subtitle. startBtn sets mission=null.
- Label "animation-pass" (2026-08-04): visual pass #3. Units lean .055rad while walking + short melee lunge (in drawUnit, after scale; ram excluded from both). Trees sway (drawn rotated ±.02rad around trunk base in the ents loop) — ONLY when `G.cam.z>=.9`; per-tree rotate at min zoom on a revealed Black Forest cost 15ms/frame, gating fixed it (sway itself ~0.8ms at normal zoom). Construction sites have 3 scaffolding stages keyed by `b.prog` (getBldSpr now takes a `stage` arg, cache key includes it; drawBld computes stage): 0=staked plot with dashed rope, 1=corner posts+frame, 2=half-raised plank walls+scaffold brace+walk plank. Lumber pile at all stages.
- Label "sprite-rework" (2026-08-04): visual pass #2 — buildings & units. New helpers after `lerpP`: `masonry(g,P,Q,h,courses)` (stone coursing + tinted blocks), `planks`, `roofLines(x0,y0,w,d,hb,rh,n)` (tile rows + lit ridge, same params as roofGable), `barrelAt/crateAt/hayAt` props, and unit-side `limb()` (outlined limbs). `drawHorse(u,t,sw,col,legC,maneC,camel)` replaces the old "table" horse — drawScout/drawKnight now call it (camel=mameluke hump; cataphract gets barding lines over it). Infantry: `legs()` has outlined legs + boots; villager/militia/spearman/archer torsos are flared-tunic paths (not ellipses), villager arms use limb(). Buildings: tc (masonry+team awning+barrel/crate), camp REDRAWN as log cabin with lean-to plank roof + log ends, farm dense crops, barracks (masonry, 2 team shields, spear rack), range (planks+arrows in target+barrel), stable (planks+hay), siege (planks+log pile), tower (flared footing, masonry), castle (masonry+arrow slits on turrets). To test art: gallery scene JS snippet is in the transcript — clear region y38-62/x2-48, place all blds/units, `G.vis.fill(2)`.
- Label "terrain-lighting-pass" (2026-08-04): AoE2-inspired visual pass #1. User supplied AoE2 screenshots as the style target (dense detail, dirt-contrast terrain, directional shadows). Done: sun-from-NW cast shadows for trees/buildings/units (`dirtPad` skewed ellipse, tree sprite sheared shadows), `mkIsoSpr` margins widened 20→56 to fit shadows, 6 tree variants (`getResSpr` key h%6, dark variants, NW highlight clumps, bigger canopies rr 10+(h%4)*1.7), 8-tone grass palette, dithered dry-dirt patches (30, in refreshTerrain), `stampWorn(b)` trampled-earth rings under finished buildings (called from refreshTerrain for existing + on construction complete; skips thin/farm). Earth tones had to be pushed BRIGHT (#a48b58..#c0a878, alpha .55+) to read against grass — first two attempts were invisible.
- Label "black-forest": battlefield chooser + Black Forest map + minimap cache.
- Prior label "visibility-fix": brighter fog/backdrop/vignette + `#homeBtn`. **CONFIRMED 2026-08-05: user says fog reads well on their phone** (after the prerendered-art-pass). Fog alphas remain protected by gotcha #4.
- Visual overhaul roadmap: ① lighting+terrain (DONE), ② animation pass (DONE), ③ sprite rework (DONE), ④ prerendered-art-pass (DONE, user-approved on phone).

## Ideas queued (user-approved direction: "match the real game / the manual")
- **DONE through 2026-08-05 (cd-update)**: garrisoning (+ inside rams), attack-move, save/load, campaign (5 missions), stone walls, market, monastery/monks/relics + **conversion + all 9 monastery techs**, control groups, **all 18 civs w/ manual bonuses + unique techs + TEAM bonuses**, up to 8 players/alliances, blacksmith + Parthian Tactics, navy, **siege lines (scorpion/mangonel/trebuchet) + gunpowder (hand cannoneer/bombard cannon/cannon galleon/bombard tower) behind Chemistry**, **University (8 techs)**, **economy techs (Loom→Thumb Ring, 16 entries)**, **farm expiry + auto-replant**, **Fish Traps**, **Wonder + Wonder/Relic victory**, petards, eagle warriors, Hun houseless play, AI tuned from the CD's SAMPLEAI.PER.
- Remaining smaller manual items: Theocracy/Sappers/Hoardings/Conscription/Spies-as-tech, Guilds/Coinage (needs a tribute system), Siege Onager & Siege Ram tiers (line arrays are age-indexed — would need a 5th slot or an explicit upgrade), Elite Cannon Galleon (skipped: age-3 units auto-elite at Imperial, would be instant).
- Other future: AI uses walls/transports/fish traps; naval attack waves; **Turbo mode** (WHATSNEW: 2.5x work rates — easy global multiplier); more campaign missions (Attila/El Cid/Montezuma themes); custom-map ideas from the CD's RMS scripts (GOODIES\RMSCRIPT — Moats, Team Glaciers etc. described in AOE2-REFERENCE.md sources).
